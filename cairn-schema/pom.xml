<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Standalone project: the repository root aggregates it but is NOT its parent.
       <relativePath/> is required — without it Maven would look for the parent at ../pom.xml.
       The Spring Boot parent is used here only for its managed liquibase/postgresql versions,
       so the changelogs are applied with exactly the versions the applications run against. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath />
  </parent>

  <groupId>com.roucoux.cairn</groupId>
  <artifactId>cairn-schema</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>cairn-schema</name>
  <description>Liquibase changelogs — schema ownership lives here, applied out-of-band (ops/pipeline), never by a running application. Depended on at test scope only, by the integration tests of the application modules.</description>

  <properties>
    <java.version>25</java.version>
    <spotless-maven-plugin.version>3.10.0</spotless-maven-plugin.version>
    <palantir-java-format.version>2.96.0</palantir-java-format.version>
    <!-- Opt-in: the seed changeset (011-seed-portfolio) carries context="seed" and is excluded by
         default. Pass -Dliquibase.contexts=seed to also apply it. -->
    <liquibase.contexts>!seed</liquibase.contexts>
    <!-- Defaults match compose.yaml's postgres service reached from the host. The Docker image
         (cairn-schema/Dockerfile) overrides these from the command line to target the compose
         network's postgres hostname instead. -->
    <liquibase.url>jdbc:postgresql://localhost:5432/app</liquibase.url>
    <liquibase.username>app</liquibase.username>
    <liquibase.password>app</liquibase.password>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.liquibase</groupId>
        <artifactId>liquibase-maven-plugin</artifactId>
        <version>${liquibase.version}</version>
        <configuration>
          <changeLogFile>src/main/resources/db/changelog/changelog-master.xml</changeLogFile>
          <url>${liquibase.url}</url>
          <username>${liquibase.username}</username>
          <password>${liquibase.password}</password>
          <contexts>${liquibase.contexts}</contexts>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <!-- No Java here, so nothing to format: declared only so that `spotless:check` run from
           the repository root resolves the plugin prefix in this project too — see AGENTS.md. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless-maven-plugin.version}</version>
        <configuration>
          <java>
            <palantirJavaFormat>
              <version>${palantir-java-format.version}</version>
            </palantirJavaFormat>
            <removeUnusedImports />
            <formatAnnotations />
          </java>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
