# Copy to /srv/proxy/.env on the server (`cp env.example .env`). One entry per application whose
# snippet in ./sites parameterizes its hostname. Caddy reads these, not the applications.
CAIRN_DOMAIN=cairn.example.com
